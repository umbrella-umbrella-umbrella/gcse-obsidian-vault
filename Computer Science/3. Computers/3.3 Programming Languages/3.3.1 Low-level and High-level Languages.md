# 3.3.1 Low-level and High-level Languages

### Instructions

- An instruction set is all the different instructions a microprocessor (CPU) can execute.
- The instructions are stored in memory as binary patterns.
- The instructions are fetched, decoded and executed by the CPU.

### Machine code

- The binary patterns, representing a program, are called machine code.
- Machine code sits as close to the hardware as possible.
- What does this program, written in machine code, do?
- While it is quite simple for the CPU to execute, it's quite a challenge for humans to understand.

### Assembly code

- As with machine code, assembly code sits very close to the hardware.
- One line of assembly code generates one instruction in machine code.
- What does this program, written in assembly code, do? MOV x0, #12 MOV x1, #8 ADD x2, x0, x1
- This is easier to understand than binary, as the mnemonics (short abbreviations) are similar to English.

### Define ‘low-level' programming language

- Sits very close to the computer's hardware
- Either machine code or assembly language
- Consists of binary codes or simple mnemonics that represent the binary codes.

### Define ‘high-level' programming language

- Abstracted away from the hardware
- The programmer does not need to understand ‘how' the computer works
- Uses English-like statements
- Requires a translator to convert the source code to machine code
- Some examples are Python, Java, JavaScript, C++ and C.

### What does this Java program output?

### Each microprocessor has its own unique

instruction set

- Microprocessor architecture is designed with a limited set of electronic components.
- An instruction set is an abstraction that defines the behaviours of these specific electronic components.
- For example, MOV x0, #12, is the abstract instruction that causes the value of 12 to be moved into register number 0.
- For each allowable and defined behaviour, there is an abstract instruction. Each microprocessor with a unique set of components requires a different unique set of instructions.

### Compare features

| Low-level languages | High-level languages |
|---|---|
| Use mnemonics or binary instructions. | Use English-like keywords. |
| One line of assembly generally generates one machine instruction. | One source line may generate several machine instructions. |
| Are specific to a processor architecture. | Are more abstract and can be portable across architectures when a suitable translator is available. |
| Have a limited range of basic instructions; complex tasks are built from small steps. | Provide constructs such as `if` statements and loops. |
| Require the programmer to manage data representation and memory directly. | Data types help organise and manage data. |

### Writing programs differs

| Low-level languages | High-level languages |
|---|---|
| Mnemonics take practice to learn and read. | English-like keywords are generally easier to read and understand. |
| The programmer constructs complex behaviour from smaller instructions. | Abstract constructs let the programmer express complex behaviour more directly. |
| The programmer needs knowledge of the processor architecture, such as registers. | The programmer does not usually need to know the hardware details. |
| Debugging can be harder because there are more small instructions to trace. | Development tools such as IDEs can support writing and debugging. |
| Can be written and tuned for fast execution, but often take longer to develop. | Usually quicker to write functional programs, especially large ones. |

### Tasks for which each is best suited

| Low-level languages | High-level languages |
|---|---|
| Situations where execution speed or fine control is important. | Programs that need to run across different processor architectures. |
| Embedded systems and device drivers. | Games, office productivity software and phone apps. |
| Real-time systems and parts of operating systems. | General-purpose applications. |
